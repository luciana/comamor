version: 1
env:
  variables:
    PROD: true
backend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands: []
frontend:
  phases:
    preBuild:
      commands:
        - npm install env-cmd
        - npm ci
    build:
      commands:
        - echo "I am running 🏃on $BUILD_ENV"
        - npm run build:$BUILD_ENV
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*